<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.pinguimzera.pinguimapp">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:name="br.com.fernandosousa.lmsapp.LMSApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SkinsActivity" />
        <activity android:name=".MapasActivity" />
        <activity android:name=".ArmasActivity" />
        <activity android:name=".TelaConfiguracaoActivity" />
        <activity android:name=".TelaInicialActivity" />
        <activity android:name=".DebugActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>